apiVersion: v1
kind: Service
metadata:
    name: "sdmlmsaks-63bd"
    labels:
        app: "sdmlmsaks-63bd"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "sdmlmsaks-63bd"